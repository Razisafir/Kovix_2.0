# Terminal Suggestions

**Notice:** This extension is bundled with CONSTRUCT IDE. It can be disabled but not uninstalled. To enable the completions from this extension, set `terminal.integrated.suggest.enabled` and `terminal.integrated.suggest.enableExtensionCompletions` to `true`.

## Features

Provides terminal suggestions for zsh, bash, fish, and pwsh.
